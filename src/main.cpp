#include <iostream>
#include <string>
#include <vector>
#include <map>
#include <algorithm>
using namespace std;

int main()
{
    string test;
    cout << test.size() << endl;
    vector<int> vc = {1, 3, 1, 3};
    map<int, int> mm;
    cout << mm.count(1) << endl;

    return 0;
}
